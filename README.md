# Iridium3D
Simple 3D viewer with basic mesh editing functions.

# Getting started.
1. Run `npm install` to install required dependencies.
2. Run `npm run dev` to start the development server.
3. Project by default runs at http://127.0.0.1:8080.
